// Command daemon runs on the user's machine and bridges local AI CLI
// sessions to the Riffpad relay.
package main

import "log"

func main() {
	log.Println("riffpad daemon: skeleton — adapter layer (Claude Code / Codex / tmux) coming next")
}
