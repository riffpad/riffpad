app = "riffpad-relay"
primary_region = "hkg"

[build]
  dockerfile = "infra/relay/Dockerfile"

[env]
  RELAY_PORT = "9090"
  RELAY_TOKEN = "change-me-to-a-long-random-token"

[http_service]
  internal_port = 9090
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
